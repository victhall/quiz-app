<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Fonts -->

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@200;300;400;500&family=Changa:wght@700;800&family=Poppins:wght@200;300;400;500&display=swap" rel="stylesheet">

  <!-- Stylesheets -->

  <link rel="stylesheet" href="/styles/navbar.css">
  <link rel="stylesheet" href="/styles/play.css">

  <title>Start Quiz</title>

</head>

<body>
  <%- include('../../views/partials/navbar.ejs') %>

  <div class="play-body">

  <h1>
    <%= trivia.title.toUpperCase() %>
  </h1>
  <section>

    <% for (let question of questions) {%>
      <div class="question">
        <%= question.question %>
      </div>
      <form action="/trivia/<%=trivia._id%>/play" method="POST" novalidate class="validate-form"
        data-toggle="validator">
        <%for (let option of question.options) {%>
          <input class="form-check-input" type="radio" name="<%= question._id%>" value="<%= option%>" required>
          <%= option %>
            </input>
            <% }%>
              <div>
              </div>
              <%}%>
  </section>
 
    <button class="submit">SUBMIT</button>
  
  </form>

</div>

  <script src="/validateForms.js"></script>

</body>

</html>